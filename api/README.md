
# National Prep DeafSports API (Optional)

Quick start:

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
uvicorn main:app --reload --port 5050
```

Then open http://localhost:5050/health
